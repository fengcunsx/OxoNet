# OxoNet — inference / analysis environment (the one that produced the released weights)
# Python 3.11, CUDA 12.1
torch==2.1.2+cu121          # --index-url https://download.pytorch.org/whl/cu121
numpy>=1.24,<2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0

# Baselines are run in their own environments (mutually incompatible torch versions):
#   esox     : torch 1.13.1+cu117, numba          -- https://github.com/marcpaga/esox
#   NanoCon  : torch 1.13.0+cu117, pytorch_lightning==1.7.1, transformers==4.6.1
#              -- https://github.com/Challis-lab/NanoCon
# Feature extraction additionally needs ont-tombo and Guppy 6.3.8 (dna_r9.4.1_450bps_sup).
